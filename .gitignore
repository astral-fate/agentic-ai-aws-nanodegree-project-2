# Credentials and account-specific config — never commit
.env
.env.local
*.pem
.aws/

# AgentCore deployment config, written by `agentcore configure`.
# Contains account IDs and ECR URIs.
.bedrock_agentcore.yaml
Dockerfile
.dockerignore

# Offline harness state (memory store between runs)
.harness-state/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.pytest_cache/
.ruff_cache/
.mypy_cache/
uv.lock

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp

# oh-my-claudecode session state
.omc/

# Downloaded/extracted submission bundles — contain live resource IDs
cs-agent-submission/
cs-agent-submission.zip

# Chrome profile used by scripts/capture_console.py — holds a console session
.aws-console-profile/
.evidence-capture-key.json
